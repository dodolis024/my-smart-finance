# Done

- 日期記錄彈窗（有記、沒記）
- 當日無消費紀錄
- 確保所有的日曆（不管彈窗還是畫面）都會自己根據當天日期跳轉，不要記憶用戶上次停在哪裡
- 在下方交易明細那裡，分類＆支付方式可以新增 filter 功能
- 新增匯率計算功能，後端自己抓每日即時匯率
- ✅ 設定自動更新匯率系統（Supabase Edge Function + pg_cron，每日台灣時間凌晨 2:00 自動更新）
- 固定新增交易區塊
- 調整金額與幣別的排列，讓金額在右側，幣別在下拉選單中
- 修正網頁邊緣比例問題
- 新增 favicon
- 修正手機版 UI 中日期欄位會超出範圍的問題
- 調整手機版 UI，讓用戶在交易明細表中可以使用滑動操作功能
- 調整手機版 streak button 位置（置頂）
- 調整手機版交易明細表格的 UI
- 新增更多按鈕（移除獨立登出按鈕）
- 可以自由新增與編輯消費、收入類別＆支付方式
