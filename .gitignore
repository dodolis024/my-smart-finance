# Supabase config (含有 API keys)
supabase-config.js
.env
.env.local

# Supabase CLI 暫存檔案
supabase/.temp/
supabase/.branches/
supabase/config.toml

# 其他常見要忽略的
node_modules/
dist/
.DS_Store
backup/

# Test artifacts
coverage/
playwright-report/
test-results/
.playwright/

# public/CHANGELOG.md 由 prebuild 從 CHANGELOG.zh.md 複製，不需納入版控
public/CHANGELOG.md

# 其他不重要的檔案
SUPABASE_MIGRATION_SUMMARY.md
MIGRATION_GUIDE.md
test.html
test-script.html
TESTING_CHECKLIST.md
to-do.md

# AI assistant workspace (local only)
.claude/
.claude/worktrees

# Implementation spec manuals from /spec skill (local only, not pushed)
docs/specs/

# Local-only manual test/seed scripts (not pushed).
# 正式單元測試放在 tests/ 底下、要進 CI，不受此規則影響。
scripts/test-*.sql
scripts/test-*.js
scripts/seed-*.sql

# Personal files
.cursor/
